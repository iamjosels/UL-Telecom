# =============================================================================
# SON-IA — Ecosistema de agentes de IA para Facturación, Cobranzas y Recaudo
# =============================================================================
# IMPORTANTE: crear el venv desde el intérprete correcto. En esta máquina
# `python` resuelve a MSYS2 (sin pandas):
#   & "C:\Users\<usuario>\AppData\Local\Programs\Python\Python311\python.exe" -m venv .venv
#
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install -r requirements.txt
#
# Estrategia de pines: SOLO el núcleo determinista va clavado a una versión
# exacta, porque de él dependen todas las cifras del demo. La capa de agentes
# va con rangos: crewai 1.15 impone pydantic>=2.11.9 y python-dotenv>=1.1, y
# clavar esas dos rompe la resolución (ResolutionImpossible).
# =============================================================================

# --- núcleo determinista (las cifras dependen SOLO de esto) -------------------
pandas==2.1.3
numpy==1.26.4                 # pandas 2.1.3 es anterior al ABI break de numpy 2.x
python-dateutil==2.8.2

# --- capa de agentes ---------------------------------------------------------
crewai[litellm]==1.15.0       # el extra [litellm] es OBLIGATORIO: sin él Groq no funciona
python-dotenv>=1.1            # rango impuesto por crewai; no fijar 1.0.x

# --- API ---------------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
sse-starlette>=2.1
pydantic>=2.11.9,<2.13        # rango impuesto por crewai; no fijar 2.9.x
python-multipart>=0.0.9       # POST /datos/cargar. Estaba llegando solo como
                              # transitiva de `mcp`; declararla es obligatorio
                              # o un cambio en el árbol de crewai deja la subida
                              # de datasets rota en tiempo de ejecución.

# --- CLI ---------------------------------------------------------------------
rich>=13.9
